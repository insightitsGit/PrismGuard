param(
    [string]$ResourceGroup = "rg-prismguard-benchmark-law"
)

$ErrorActionPreference = "Stop"
Write-Host "Fetch results from storage/ACI logs for $ResourceGroup (implement after first Azure run)."
